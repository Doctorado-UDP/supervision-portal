# Netlify preview note

PR deploy preview should be used for UI validation only. Auth email links default to the production portal URL unless the hosted Supabase Auth redirect configuration permits the preview URL and the email template uses the redirect target.

Temporary file; remove before merge.
