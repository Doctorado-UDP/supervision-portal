# Beta.4 review notes

The linked Supabase project already has the beta.4 access-management migrations applied and `invite-supervision-user` Edge Function version 2 deployed with JWT verification.

The remaining environment-specific item before merge is to confirm the hosted Supabase Auth URL/rate-limit settings match `supabase/config.toml`. The repository file itself is already production-oriented.

Temporary beta.4 review files under `docs/` will be removed before merge.
